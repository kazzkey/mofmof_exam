<%= form_with(model: near_station, local: true) do |form| %>
  <% if near_station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(near_station.errors.count, "error") %> prohibited this near_station from being saved:</h2>

      <ul>
      <% near_station.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :route_name %>
    <%= form.text_field :route_name %>
  </div>

  <div class="field">
    <%= form.label :station_name %>
    <%= form.text_field :station_name %>
  </div>

  <div class="field">
    <%= form.label :minute_walk %>
    <%= form.number_field :minute_walk %>分
  </div>
<% end %>
