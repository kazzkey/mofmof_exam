<p id="notice"><%= notice %></p>

<h1>Near Stations</h1>

<table>
  <thead>
    <tr>
      <th>Route name</th>
      <th>Station name</th>
      <th>Minute walk</th>
      <th>Property</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @near_stations.each do |near_station| %>
      <tr>
        <td><%= near_station.route_name %></td>
        <td><%= near_station.station_name %></td>
        <td><%= near_station.minute_walk %></td>
        <td><%= near_station.property %></td>
        <td><%= link_to 'Show', near_station %></td>
        <td><%= link_to 'Edit', edit_near_station_path(near_station) %></td>
        <td><%= link_to 'Destroy', near_station, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Near Station', new_near_station_path %>
